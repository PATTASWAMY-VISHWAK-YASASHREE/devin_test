# GitHub token from OAuth device flow (set after running login)
GITHUB_TOKEN=

# API key to protect this proxy (clients must send this as Bearer token)
PROXY_API_KEY=changeme

# Server settings
HOST=0.0.0.0
PORT=8000
